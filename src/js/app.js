import Button from './Button';
import Chat from './Chat';
import Like from './Like';

const button = new Button();
button.init();

const chat = new Chat();
chat.init();

const like = new Like();
like.init();
